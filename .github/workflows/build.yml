name: Scheduled Job

on:
  schedule:
    - cron: "1 * * * *"
    

jobs:
  scheduled-job:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
       echo "No changes to commit"
